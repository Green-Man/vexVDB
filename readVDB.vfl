surface readVDB(string filepath = "/home/green/Downloads/smoke.vdb")
{
    vector val = 0;
    vector pWorld = ptransform("space:world", P);
    val = readVDB( filepath, pWorld );

    Cf = val;
    Of = 1;//luminance(val);//*diffuse(N);
}