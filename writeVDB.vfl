fog writeVDB(	string filePath = "/home/green/Downloads/data.vdb";
				vector Cd = 1)
{
	int handle = openVDB(filePath, 1);
    
    if(Pz < 1000){
	    vector pWorld = ptransform("space:world", P);    
	    writeVDB(handle, pWorld, Cf);
    }


    Of = Of;
    Cf = Cf;//*diffuse(N);
}